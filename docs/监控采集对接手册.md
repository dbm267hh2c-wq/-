# 监控采集对接手册

请按本文完成信息填报，并在有日志的业务服务器上安装 **Filebeat**。  
**不要安装** Metricbeat、Heartbeat、Logstash、Elasticsearch、Kibana。

需要完成两件事：

1. **填表回传**：服务器、应用端口、接口地址、数据库、调用链路
2. **安装 Filebeat**：把应用日志报到指定地址

IP、端口、接口地址、数据库地址、调用关系缺一项，后续监控无法配置。

---

## 1. 上报地址与版本

| 项目 | 值 |
| --- | --- |
| Filebeat 上报地址 | `172.28.65.248:5044`（TCP） |
| 加密 | 当前未开启 SSL，不要配置证书 |
| Filebeat 版本 | **8.x**，建议 `8.19.19`，不要安装 7.x / 9.x |

```
业务服务器
  应用日志 ──► Filebeat ── TCP 5044 ──► 172.28.65.248
```

请先确认网络：

```bash
nc -vz 172.28.65.248 5044
# 或
bash -c 'echo >/dev/tcp/172.28.65.248/5044' && echo OK || echo FAIL
```

不通时先放行 **TCP 5044**，不要反复重启 Filebeat。

无法安装 Filebeat、只能发 syslog 的设备，将日志发到 `172.28.65.248:514/udp`。同一份日志不要 Filebeat 和 UDP 各发一次。

---

## 2. 请填写并回传的信息

可直接改本文件，或导出为 Excel。表格可增行。

### 2.1 服务器清单

| 序号 | 环境 | 主机名 | 业务 IP | 操作系统及版本 | 角色（应用/DB/中间件） | 是否安装 Filebeat | 日志路径 | 责任人 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 生产 |  |  |  |  | 是 / 否 |  |  |  |
| 2 |  |  |  |  |  |  |  |  |  |

说明：

- 业务 IP 将作为 Filebeat 配置里的 `name`，必须唯一。
- 不装 Filebeat 的机器（托管库、网络设备等）请在备注写明原因。

### 2.2 应用与端口

| 序号 | 应用名称 | 部署主机 IP | 协议（HTTP/TCP/gRPC） | 监听端口 | 健康检查 URL 或探测方式 | 对外 / 对内 | 关联数据库 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 |  |  |  |  | 例：`GET /health` 或 TCP |  |  |  |
| 2 |  |  |  |  |  |  |  |  |

没有端口和健康检查路径，无法确认服务是否真正可用。  
**端口通不等于接口可用。** 业务接口请另外填第 2.3 节，必须写完整 URL。

### 2.3 接口地址（必填，需检查）

请提供需要监控的全部业务接口，一行一个 URL。不要只写 IP 和端口。

| 序号 | 接口名称 | 所属应用 | 完整接口地址（含协议/域名或IP/端口/路径） | 方法 | 是否鉴权 | 期望状态码 | 期望响应关键字（可选） | 超时（秒） | 内外网 | 是否核心 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 订单健康检查 | 订单服务 | `http://10.0.0.21:8080/health` | GET | 否 | 200 | `"status":"UP"` | 5 | 内网 | 是 |  |
| 2 | 下单接口 | 订单服务 | `http://10.0.0.11:8080/api/order/create` | POST | 是 | 200 |  | 5 | 内网 | 是 | 检查请用只读或空跑接口 |
| 3 | 库存查询 | 库存服务 | `http://10.0.0.22:8080/api/stock/query` | GET | 否 | 200 |  | 5 | 内网 | 是 |  |
| 4 |  |  |  |  |  |  |  |  |  |  |  |

填写要求：

- 必须是可直接访问的完整地址，例如 `http://IP:端口/path` 或 `https://域名/path`，不要只写 `/api/xxx`。
- 健康检查、查询类接口优先；写操作、支付类不要作为常规检查，或提供专门的探测/空跑接口。
- 需要 Header、Token 的接口，在备注写明检查方式（固定探测账号 / 免鉴权健康检查路径），**不要把生产密钥写进本表长期留存**，密钥走加密通道。
- 期望状态码一般填 `200`。若接口正常时返回 `204` 或业务码在 JSON 里，请写清楚。

填完后，请在能访问该接口的机器上逐条检查，并把结果一并回传：

```bash
# GET 示例，应返回期望状态码
curl -sS -o /tmp/api_body.txt -w "http_code=%{http_code} time=%{time_total}\n" \
  "http://10.0.0.21:8080/health"

# 需要 Header 时
curl -sS -o /tmp/api_body.txt -w "http_code=%{http_code}\n" \
  -H "Content-Type: application/json" \
  "http://10.0.0.22:8080/api/stock/query"

# HTTPS
curl -sS -o /tmp/api_body.txt -w "http_code=%{http_code}\n" \
  "https://example.com/api/health"
```

`http_code` 与表格中的期望状态码不一致，视为接口不可用，请先修复再回传。

可按下表记录检查结果：

| 序号 | 接口名称 | 检查时间 | http_code | 耗时（秒） | 是否符合期望 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 |  |  |  |  |  |  |
| 2 |  |  |  |  |  |  |

### 2.4 数据库

| 序号 | 类型（MySQL/PG/Oracle/SQLServer/Redis/Mongo 等） | 业务用途 | IP | 端口 | 库名 / 实例名 | 只读账号 | 是否主从 | 所在主机 | 被哪些应用使用 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 |  |  |  |  |  |  |  |  |  |
| 2 |  |  |  |  |  |  |  |  |  |

说明：

- IP、端口、库名必填。
- 如需提供监控账号，只给 **只读** 账号，不要给业务高权账号。
- 密码请走加密通道回传，不要长期放在邮件正文。

填完后请检查数据库端口是否可通（在能访问该库的机器上执行）：

```bash
nc -vz 数据库IP 端口
# 例：nc -vz 10.0.0.31 3306
```

### 2.5 中间件（如有）

| 序号 | 类型（Nginx/Kafka/RabbitMQ/Nacos 等） | IP | 端口 | 管理端口 | 用途 | 关联应用 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 |  |  |  |  |  |  |
| 2 |  |  |  |  |  |  |

### 2.6 应用之间的调用链路（必填）

请架构或开发填写，一行一条调用。

| 序号 | 调用方应用 | 调用方 IP:端口 | 被调应用 | 被调 IP:端口 | 协议 | 路径或接口 | 同步/异步 | 是否核心链路 | 失败影响 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 网关 | 10.0.0.11:8080 | 订单服务 | 10.0.0.21:8080 | HTTP | `/api/order/**` | 同步 | 是 | 下单失败 |
| 2 | 订单服务 | 10.0.0.21:8080 | 订单库 | 10.0.0.31:3306 | MySQL | `order` | 同步 | 是 | 写库失败 |
| 3 | 订单服务 | 10.0.0.21:8080 | 库存服务 | 10.0.0.22:8080 | HTTP | `/api/stock/lock` | 同步 | 是 | 超卖风险 |
| 4 | 订单服务 | 10.0.0.21:8080 | Kafka | 10.0.0.41:9092 | MQ | `topic_order` | 异步 | 否 | 下游延迟 |

请同时提供文字拓扑（示例，请改成实际环境）：

```
用户 → Nginx/网关:8080 → 订单服务:8080 ┬→ MySQL:3306（订单库）
                                      ├→ 库存服务:8080 → Redis:6379
                                      └→ Kafka:9092 → 通知服务:8080
```

链路上的 HTTP 接口都应出现在第 2.3 节接口表中。

### 2.7 权限确认

| 项目 | 确认（是/否） | 说明 |
| --- | --- | --- |
| 业务机可访问 `172.28.65.248:5044/tcp` |  | Filebeat 上报 |
| 允许安装 Filebeat |  | 不能装的机器单独列出 |
| 已提供全部接口完整 URL，并用 `curl` 检查 |  | 见第 2.3 节 |
| 已提供数据库 IP、端口 |  | 见第 2.4 节 |
| 已提供应用间调用关系 |  | 见第 2.6 节 |

---

## 3. 安装 Filebeat

只装 Filebeat，不要再装其他采集器或转发组件。

### 3.1 CentOS / RHEL / 麒麟

```bash
rpm --import https://artifacts.elastic.co/GPG-KEY-elasticsearch

cat > /etc/yum.repos.d/elastic-8.x.repo << 'EOF'
[elastic-8.x]
name=Elastic repository for 8.x packages
baseurl=https://artifacts.elastic.co/packages/8.x/yum
gpgcheck=1
gpgkey=https://artifacts.elastic.co/GPG-KEY-elasticsearch
enabled=1
autorefresh=1
type=rpm-md
EOF

yum install -y filebeat-8.19.19
```

无外网时，下载对应 `rpm` 拷到服务器安装：

```bash
rpm -ivh filebeat-8.19.19-x86_64.rpm
```

### 3.2 Ubuntu / Debian

```bash
wget -qO - https://artifacts.elastic.co/GPG-KEY-elasticsearch | gpg --dearmor -o /usr/share/keyrings/elastic-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/elastic-keyring.gpg] https://artifacts.elastic.co/packages/8.x/apt stable main" \
  > /etc/apt/sources.list.d/elastic-8.x.list
apt-get update
apt-get install -y filebeat=8.19.19
```

### 3.3 安装后立刻关掉模块

```bash
filebeat modules disable system nginx mysql 2>/dev/null || true
systemctl stop filebeat
```

---

## 4. 配置 Filebeat（只改 3 处）

配置文件：`/etc/filebeat/filebeat.yml`

```bash
cp /etc/filebeat/filebeat.yml /etc/filebeat/filebeat.yml.bak.$(date +%F)
```

将第 5 节内容覆盖进去后，只改：

| 配置项 | 怎么填 |
| --- | --- |
| `paths` | 本机真实日志路径，例如 `/app/logs/*.log` |
| `name` | 本机业务 IP（与服务器清单一致） |
| `output.logstash.hosts` | 保持 `["172.28.65.248:5044"]` |

不要打开 `output.elasticsearch`，不要配置 Kibana，不要开启 SSL。  
机器标识请写在 `name`，不要指望 `fields`、`tags` 作为来源标识。

### 4.1 多行日志

当前规则：行首为 `YYYY-MM-DD` 视为一条新日志，后续堆栈合并到上一条。

日志格式不同时，必须改 `parsers.multiline.pattern`，否则堆栈会被拆碎或粘成一条。

| 日志行首 | pattern |
| --- | --- |
| `2026-09-18 12:00:01` | `'^\d{4}-\d{2}-\d{2}'` |
| `2026/09/18` | `'^\d{4}/\d{2}/\d{2}'` |
| `[2026-09-18 12:00:01]` | `'^\[\d{4}-\d{2}-\d{2}'` |
| 单行日志、无堆栈 | 删除整个 `parsers` 段 |

### 4.2 注意

- `filestream` 的 `id` 用来记采集进度。同一批文件 **不要改 id**，也不要删除 `/var/lib/filebeat/registry`，否则会把历史日志重发一遍。
- Filebeat 必须能 **读取** 日志文件。常见失败是 `permission denied`。
- 一台机器只运行一个 Filebeat，不要两个进程盯同一批文件。
- 不要采集未经约定的 `/var/log/messages` 全量系统日志。

日志读权限示例：

```bash
setfacl -m u:filebeat:rx /data/monitor/server/logs
# 或
usermod -aG <日志所属组> filebeat
systemctl restart filebeat
```

---

## 5. 可直接使用的 `filebeat.yml`

```yaml
filebeat.inputs:
- type: filestream
  id: app-logs
  enabled: true
  paths:
    - /data/monitor/server/logs/*.log   # 【必改】实际日志路径
  ignore_older: 72h
  parsers:
    - multiline:
        type: pattern
        pattern: '^\d{4}-\d{2}-\d{2}'   # 必须单引号；按实际日志行首修改
        negate: true
        match: after

filebeat.config.modules:
  path: ${path.config}/modules.d/*.yml
  reload.enabled: false

name: "请改成本机业务IP"

setup.template.enabled: false
setup.ilm.enabled: false
setup.dashboards.enabled: false
setup.kibana.enabled: false

output.elasticsearch:
  enabled: false

output.logstash:
  enabled: true
  hosts: ["172.28.65.248:5044"]
  bulk_max_size: 2048
  worker: 2
  compression_level: 3
  ttl: 30s
  pipelining: 2

processors:
  - add_host_metadata:
      when.not.contains.tags: forwarded
  - copy_fields:
      fields:
        - from: agent.name
          to: host.name
      fail_on_error: false
      ignore_missing: true
  - drop_fields:
      fields: ["cloud", "docker", "kubernetes"]
      ignore_missing: true

logging.level: info
logging.to_files: true
logging.files:
  path: /var/log/filebeat
  name: filebeat
  keepfiles: 7
  permissions: 0640
```

---

## 6. 启动与自检

```bash
filebeat test config -c /etc/filebeat/filebeat.yml
filebeat test output -c /etc/filebeat/filebeat.yml
systemctl enable --now filebeat
journalctl -u filebeat -f
```

`test output` 成功时应能连上 `172.28.65.248:5044`。  
日志中出现 `Harvester started for file` 表示已开始采集。

---

## 7. 建议实施顺序

1. 填完第 2 节全部表格（尤其是接口地址、调用链路）。
2. 用 `curl` 检查第 2.3 节全部接口，用 `nc` 检查端口和数据库端口。
3. 确认可以访问 `172.28.65.248:5044`。
4. 在有日志的应用机安装并启动 Filebeat。

---

## 8. 提交前检查清单

- [ ] 服务器清单：主机名、业务 IP、操作系统、角色、是否安装 Filebeat
- [ ] 每个应用都有 IP、端口、健康检查方式
- [ ] 每个需监控的接口都有完整 URL、方法、期望状态码
- [ ] 第 2.3 节每个接口已用 `curl` 检查，状态码符合预期
- [ ] 每个数据库都有类型、IP、端口、库名
- [ ] 调用关系表覆盖核心链路，并附文字拓扑
- [ ] 调用链上的 HTTP 接口均已列入第 2.3 节
- [ ] 不安装 Filebeat 的机器已单独列出原因
- [ ] 仅安装 Filebeat，未安装 Metricbeat / Heartbeat
- [ ] Filebeat 为 8.x
- [ ] 输出为 `172.28.65.248:5044`，Elasticsearch 输出已关闭
- [ ] `name` 已改成本机业务 IP
- [ ] 多行规则与日志行首格式一致
- [ ] `filestream.id` 固定，不会再改
- [ ] 未开启 SSL
- [ ] Filebeat 对日志文件有读权限
- [ ] `filebeat test config`、`filebeat test output` 均成功
- [ ] 未同时用 Filebeat 和 UDP 514 发送同一份日志

---

## 9. 常见问题

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| `connection refused` / `test output` 失败 | 5044 未放行，或地址填错 | 核对 `172.28.65.248:5044`，先做 `nc -vz` |
| `i/o timeout` | 中间防火墙丢包 | 明确放行 TCP 5044 |
| 启动后无日志 | 路径不对、没有新日志、被 `ignore_older` 过滤 | 确认文件有新行；可临时去掉 `ignore_older` |
| `permission denied` | 读不到日志文件 | ACL 或把 filebeat 用户加入日志组 |
| 堆栈被拆成多条 / 多条粘成一条 | 多行 pattern 不匹配 | 按 4.1 修改 |
| 历史日志突然全量重发 | 改了 `filestream.id` 或清理了 registry | 不要改 id，不要删 registry |
| 端口通但接口 500/超时 | 只看了端口，未检查完整 URL | 按 2.3 用 `curl` 复核完整接口地址 |
| 接口检查 401/403 | 未提供免鉴权健康检查或探测账号 | 补健康检查路径，或走加密通道提供探测凭证 |
| 接口检查一直失败 | URL 不完整、只能内网访问、证书问题 | 填完整 `http(s)://主机:端口/path`；在能访问的机器上执行 `curl` |

---

## 10. 请不要做的事

1. 不要安装 Metricbeat、Heartbeat。
2. 不要把输出改成 Elasticsearch。
3. 不要再安装一套 Logstash 做中转。
4. 不要混用 7.x / 9.x Filebeat。
5. 不要开启 Docker / Kubernetes 自动发现。
6. 不要把 `bulk_max_size` 开得过大。
7. 同一份日志不要 Filebeat 和 UDP 各发一次。
8. 不要用下单、支付、写库类接口做常规检查，改用健康检查或只读查询接口。

---

## 11. 完成后请回传

1. 填好的第 2 节表格（可直接改本文件，**含第 2.3 节接口地址及 curl 检查结果**）。
2. 调用关系拓扑图（或第 2.6 节文字图）。
3. 数据库 IP、端口（账号如提供，走加密通道）。
4. `filebeat version`。
5. `filebeat test output` 完整输出。
6. 配置的 `name` 和日志路径。
7. 不安装 Filebeat 的主机及原因。
